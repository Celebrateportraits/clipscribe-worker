{
  "name": "clipscribe-worker",
  "version": "1.0.0",
  "description": "External worker for ClipScribe AI — downloads videos with yt-dlp, extracts frames + audio with ffmpeg, transcribes with Groq Whisper.",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=20"
  }
}
